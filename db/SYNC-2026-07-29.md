# Sync histórico (2026-07-29)
#
# Comparó el proyecto *anterior* `oxlmqzheogkharxpqjwp` contra el Docker local.
# La fuente de verdad actual de contenido es `roxbekpxdgvqbosepmdd`
# (cuenta palabrapuraia@gmail.com). Ver CONEXION.md.

Compared project `oxlmqzheogkharxpqjwp` (legacy)
against the local Docker DB `palabra_pura` **without deleting any local data**.

## Data

| Table | Supabase | Server (before) | Action |
|-------|----------|-----------------|--------|
| video | 226 | 226 | Identical (`youtube_id` set matched) |
| fragment | 2592 | 2592 | Identical |
| bible_chunks | 10364 | 10364 | Identical |
| bible_parents | 1190 | 1190 | Identical |
| verses / books | same | same | Identical |
| error_log | 968 | 966 | Added rows 972–973 only |

## Schema (additive)

- Added function `public.match_bible_chunks(...)` — used by the updated chatbot Edge Function as Bible fallback when videos don’t answer.
- Added RLS policy `lectura publica books` if missing.

SQL applied: `db/migrations/20260729_add_match_bible_chunks.sql`

## Edge Functions (downloaded)

Extracted TypeScript (not deployed into Docker; Supabase still hosts them):

- `supabase/functions/chatbot-iglesia-palabra-pura/index.ts` (v10 — video first, then bible chunks)
- `supabase/functions/create-fragment/index.ts`

The public chat UI continues to call the Supabase Edge Function URL in `web/app/js/config.js`.
